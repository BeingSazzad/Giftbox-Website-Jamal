/**
 * Global state entry point.
 *
 * Currently the app uses React Context (AuthProvider) for auth state.
 * As the app grows, replace or extend this with Zustand or Redux Toolkit:
 *   - `npm install zustand`     → lightweight, recommended for this project
 *   - `npm install @reduxjs/toolkit react-redux`  → full Redux if needed
 *
 * Phase 5 of the refactor will wire state management here.
 */

export {}
